# GET /api/investigations/assigned-reports
# Requires: PERF_TOKEN_AUTHORIZED_PERSON

config:
  target: "http://localhost:5000"
  http:
    timeout: 60

  phases:
    - duration: 30
      arrivalRate: 3
      name: "steady"
  plugins:
    ensure: {}
  ensure:
    thresholds:
      - http.response_time.p95: 15000
      - http.response_time.p99: 25000
scenarios:
  - name: "GET /api/investigations/assigned-reports"
    flow:
      - get:
          url: "/api/investigations/assigned-reports"
          headers:
            Authorization: "Bearer {{ $environment.PERF_TOKEN_AUTHORIZED_PERSON }}"
