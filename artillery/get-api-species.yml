# GET /api/species — paginated
# Requires: PERF_TOKEN_PUBLIC_USER

config:
  target: "http://localhost:5000"
  http:
    timeout: 60

  phases:
    - duration: 30
      arrivalRate: 3
      name: "steady"
  plugins:
    ensure: {}
  ensure:
    thresholds:
      - http.response_time.p95: 15000
      - http.response_time.p99: 25000
scenarios:
  - name: "GET /api/species"
    flow:
      - get:
          url: "/api/species?page=1&limit=10"
          headers:
            Authorization: "Bearer {{ $environment.PERF_TOKEN_PUBLIC_USER }}"
