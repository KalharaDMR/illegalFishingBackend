# GET /api/reports/district/:district
# Requires: PERF_TOKEN_ADMIN, PERF_DISTRICT_NAME

config:
  target: "http://localhost:5000"
  http:
    timeout: 60

  phases:
    - duration: 30
      arrivalRate: 3
      name: "steady"
  plugins:
    ensure: {}
  ensure:
    thresholds:
      - http.response_time.p95: 15000
      - http.response_time.p99: 25000
scenarios:
  - name: "GET /api/reports/district/:district"
    flow:
      - get:
          url: "/api/reports/district/{{ $environment.PERF_DISTRICT_NAME }}"
          headers:
            Authorization: "Bearer {{ $environment.PERF_TOKEN_ADMIN }}"
